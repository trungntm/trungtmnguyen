---
description: References AGENTS.md as the canonical repository constitution.
trigger: always_on
---

# Repository Guidance

Follow the repository-wide instructions defined in `AGENTS.md`.

Treat `AGENTS.md` as the canonical source for:

- repository structure;
- architecture and dependency rules;
- CodeGraph usage;
- development philosophy;
- scope control;
- coding conventions;
- verification requirements;
- definition of done.

The source code remains the final source of truth.

Do not duplicate the full contents of `AGENTS.md` in Antigravity rules.

Before implementation:

1. Read the relevant sections of `AGENTS.md`.
2. Use CodeGraph when required by its repository exploration policy.
3. Inspect the identified source files directly.
4. Apply any additional context-specific workspace rules.
