# Prettier Config

The root `.prettierrc` is the active source of truth for this phase. This folder is reserved for extracting shared Prettier presets once additional workspace packages need their own package-level config entry points.
